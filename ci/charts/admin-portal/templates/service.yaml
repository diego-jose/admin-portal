apiVersion: v1
kind: Service
metadata:
  name: admin-portal
  labels:
    app: admin-portal
    release: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      protocol: TCP
      targetPort: port-shell-app
  selector:
    app: admin-portal
    release: {{ .Release.Name }}